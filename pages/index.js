import {
  Form,
  Select,
  InputNumber,
  DatePicker,
  Switch,
  Slider,
  Button,
} from 'antd'

import Layout from '../components/Layout'

const FormItem = Form.Item
const Option = Select.Option

export default () => (
  <Layout></Layout>
)
